Application port should be 3000, the reason application configured 3000 port on code level.

when we run docker build and docker run comamnds jenkinsfile1, if application is not running , perform below command on server.

sudo ufw allow 3000

